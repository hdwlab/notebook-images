---
- name: Install development tools
  become: true
  ansible.builtin.apt:
    update_cache: yes
    name:
      - bzip2
      - curl
      - dnsutils
      - git
      - htop
      - iftop
      - iputils-ping
      - less
      - nano
      - nmon
      - openssh-client
      - tmux
      - unzip
      - vim
      - wget
      - zip
  notify:
    - dev-tools installation is done
